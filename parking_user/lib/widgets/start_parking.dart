import 'package:flutter/material.dart';

class StartParking extends StatelessWidget {
  const StartParking({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Starta parkering'));
  }
}
