import 'package:flutter/material.dart';

class PersonalSettings extends StatelessWidget {
  const PersonalSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Inställningar');
  }
}
